export class Person {
    construction(firstName, lastName, iNumber, location) {
        this.firstName = first.Name,
        this.lastName = lastName,
        this.iNumber = iNumber,
        this.location = location
    }
}